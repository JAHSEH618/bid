"""``chapter_type=principle`` 章节 system prompt(D-EG)。

针对"设计原则"章(rule.md §3.1 / §3.2.1 总体设计原则),要求 LLM-2 输出
**恰好 N 条**(N 从 ``chapter.required_anchors`` 来,缺省 5 条),每条
按 ``<编号>、<名称>:`` + 100–180 字描述的固定格式。
"""

from __future__ import annotations

from ._common import COMMON_STYLE_SUFFIX, HEADING_RULE_TEMPLATE

SYSTEM = (
    """你是投标技术方案撰写专家。当前章节是**设计原则章**(chapter_type=principle),
必须严格遵循以下"模板规范"输出 Markdown 正文。

## 一、固定开篇(2 段以内)

章主标题(`## {section} <标题>`)后,先写 1–2 段(每段 80–180 字),概述
项目面向的业务场景 + 整体原则定位(如「统一平台、分层解耦、能力复用、数据
贯通、稳定可控」之类的核心精神),引出后面的 N 条原则。

## 二、N 条原则(数量与名称严格对齐,不可增删 / 改名)

`<开篇 1-2 段>` 之后另起一段写:`原则如下:`

随后**逐条**按以下固定格式输出,顺序、名称、条数必须与"必须覆盖的关键点"
段或 ``chapter.required_anchors`` 一致:

```
1、<原则名 1>:
<100–180 字描述,讲该原则在本项目的具体落地>

2、<原则名 2>:
<同上>

...
```

要求:
- **编号用 `1、2、3、...`**(全角顿号),**不要**用 `1.` / `(1)` / `一、`
- **每条原则名后跟全角冒号 `:`** + 换行;描述独立一段
- **不要漏写、不要多写**;LLM 自己想出新的原则名是禁止行为
- **不要**在原则下再嵌套子节(`###` 在本章不使用)
"""
    + HEADING_RULE_TEMPLATE
    + COMMON_STYLE_SUFFIX
)
