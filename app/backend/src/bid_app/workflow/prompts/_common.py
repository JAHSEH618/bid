"""LLM-2 系列 prompt 共用片段(D-EG / D-EJ)。

把"排版 / Mermaid / 占位符 / 文体黑名单"等所有章节类型都该遵守的规则集中到
一个常量里,由 ``write_module_prompt`` / ``write_principle_prompt`` /
``write_architecture_prompt`` / ``write_meeting_prompt`` / 旧
``write_chapter_prompt``(``normal`` 兜底)各自拼接进 system,避免 5 套
prompt 维护漂移。
"""

from __future__ import annotations

COMMON_STYLE_SUFFIX = """
## 排版规范(必须遵守,否则渲染器分段失败)

- **段落之间必须有一个空行**(用 `\\n\\n` 分隔),不允许两段紧挨
- **标题前后**(`##` / `###` / `####`)必须有空行
- **列表 / 代码块 / 表格** 与上下段落之间必须有空行
- 不要连续 3 个以上空行;1 个空行(`\\n\\n`)足够分段
- 中英文 / 数字混排时,数字前后加半角空格:`15 分钟`、`SLA 不低于 99.5%`(不是 `15分钟`)
- 长段落(>200 字)考虑拆分:用 `###` 子节标题分段,或用列表逐项展开

## 文体硬规范(D-EJ,违反即被自动拒收重写)

1. **列表编号白名单**:顶层用 `1. xxx` 或 `1、xxx`;二级用 `（1）xxx`
   ❌ 禁止使用:`一、二、` / `①②③` / `◆▶●■` / `✓✗` / 任何 emoji
2. **段长 ≤ 180 字**;超长用 `###` 子节或编号列表拆分
3. 不写感叹号、营销口号、未量化的形容词(`非常优秀` `极大提升` `完美` `极致`)
4. 引号统一用中文双引号 `"…"`,不用 ASCII `'/"`
5. 不写"以下是..."、"本章将..."等元描述,直接进入正文
6. 不写"以上即为本章内容"等总结句
7. 不写"如下表所示""见附录"等指代性表述,直接把表 / 图放到对应位置

## Mermaid 图表(若需要在正文里插入流程 / 架构图)

- 流程、架构、层级、模块关系图**只允许 Mermaid**,不要用 `+---+`、竖线、横线拼 ASCII 框图
- 用 ` ```mermaid ... ``` ` 围栏,**不要**自定义节点配色(不要写 `style A fill:#xxx` / `class A;` 等装饰),让前端 mermaid theme 接管
- Mermaid 代码块必须独立成段:开头 ` ```mermaid ` 单独一行,结尾 ` ``` ` 单独一行,图表前后各保留一个空行
- Mermaid 代码块内只写图表语法,不要在围栏内夹正文解释;图表说明写在代码块前后的普通段落中
- 不要把 ` ```mermaid `、`flowchart TD`、结束围栏写在同一行
- 节点标签内含中文用 `["中文 内容"]` 双引号包(防止老版本语法报错)
- 优先用 `flowchart TD` / `flowchart LR` / `sequenceDiagram` / `classDiagram` 等通用语法
- 中文标签控制在 8 字内,避免节点过大;长描述写在 `note` 里

## 占位符规则(D3)

文中形如 `__ORG_xxxxxx__` `__PROJ_xxxxxx__` `__PERSON_xxxxxx__` `__PHONE_xxxxxx__`
`__EMAIL_xxxxxx__` `__IDCARD_xxxxxx__` 的标记是占位符,代表被脱敏的敏感信息。
请保留原占位符原样,不必复述,也不要替换为具体公司名 / 项目号 / 人名 / 电话。
下游会在导出时由人工核对。

## Revise 模式

如果用户提示词中包含上一轮的修改建议,严格按建议修订:
- 局部调整("第 X 段加 Y" / "把表格改流程图")→ 保持其他部分基本一致
- 整体问题("内容空泛" / "逻辑混乱")→ 做实质性重组
- 不简单换措辞或表面微调
- 输出完整修订后的本章 Markdown,不是 diff
"""


HEADING_RULE_TEMPLATE = """
## Markdown 标题层级(严格遵守)

- 章节主标题用 **`## {section} {title}`**(二级标题)
- 子节用 **`### {section}.N <子节标题>`**(三级标题)
- 更细分用 **`#### {section}.N.M <小节标题>`**(四级标题,可选)
- **不要**用一级标题 `#`(渲染器会与全文标题冲突)
- **不要**多个 `##` 平级排列(章主用 `##`,子节必须降到 `###`)
- **不要**自创章节编号(如 `## 第一章`);严格使用 ``section`` 字段值开头
"""
